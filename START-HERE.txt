ARKA V46

1. Extract this project.
2. Run:
   supabase/migrations/20260916_0004_arka_onboarding_schema_compat.sql
   in Supabase SQL Editor.
3. Reload the app.
4. Sign in as the Tenant Admin and test Save & continue.

For frontend-only changes during local development, use Vite directly (npm run dev)
so source changes hot-reload. If using Docker, see docker-compose.yml and use a
bind-mounted source directory/dev container rather than rebuilding the image for
every source edit.
