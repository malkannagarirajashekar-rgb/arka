# V64.4 Screenshot QA Matrix

## Screenshot coverage added

The Playwright suite now captures:

### Public home
- full-page desktop
- full-page mobile
- after-scroll desktop
- light desktop/mobile
- individual screenshots for Hero, Issue, Method, Surfaces, Context, Trace, Principle, Guardian Gate, and Contact

### Login
- full-page desktop/mobile
- light desktop/mobile
- after-scroll desktop

### Authenticated routes
- full-page desktop/mobile
- after-scroll desktop
- light desktop/mobile
- section-level screenshots for header, welcome/hero, context, metrics, and main content where present

## V64.4 visual contracts

- `/app` must render `.workspace-context` instead of `.workspace-temple-strip`.
- `/app` must contain no `ARKA / SANCTUM` or `GUARDIAN / CORE` workspace text.
- Tenant User context nodes use rounded interactive controls.
- Public Context and Guardian Gate no longer use rectangular panel borders.
- Public live context uses the new organic orbital composition.

## Environment result

Static architecture and CSS audits pass for this source snapshot.

Browser/build execution could not be completed in this environment because the ZIP does not contain `node_modules` and `npm install` timed out while resolving packages. The project source and Playwright tests were still updated so the same screenshot matrix runs when dependencies are installed.
